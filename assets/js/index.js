function darkMode() {
    var element = document.body;
    element.classList.toggle("dark-mode");
}
function menu(x) {
    x.classList.toggle("change");
    var navmenu = document.body;
    navmenu.classList.toggle("menu-active");
}